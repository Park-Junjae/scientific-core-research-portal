# First Run Migration Report

## Decision

No actual scientific asset is imported. Explicit public approval applies only to three synthetic demonstration bundles. All scientific source trees remain unchanged.

## Priority inventory

| Source run | Frozen state observed | Size | Classification | Required action |
|---|---|---:|---|---|
| `xrRNA_prime_assembly_KO_text_final_v4_1_1` | Completed bilingual editorial package with Korean Knowledge Background and Idea Report PDFs | 7,679,073 B | `REQUIRES_REDACTION` | PI selects exact PDFs/Markdown/BibTeX and approves redistribution; exclude QA/audit internals by default. |
| `prame_logic_first_transfer_v1` | Human decision retains four causal-identification leads; no candidate promotion or therapeutic prioritization | 18,875,269 B | `REQUIRES_REDACTION` | Build a reader report from approved decision/background artifacts; exclude `_work`, raw routes, provider workflow, private evaluation context, and registries. |
| `scientific_core_taled_canonical_live_v2_3` | Blocked before Stage 4 by execution budget; no finalist/support object, Arena, or user rating | 2,756,135 B | `HISTORICAL_ONLY` | Publish only a redacted quality-learning case if explicitly approved; do not expose raw outputs, requests, receipts, registries, token/cost data, or imply an active recommendation. |

## Full run-root policy

- Hidden build directories, `worktrees`, `reports`, `runtime_outputs`, input staging, refreeze/sync directories: `DO_NOT_PUBLISH`.
- Runtime v2 benchmarks, canaries, qualification runs, forensic runs, contract smokes, role-memory audits: `DO_NOT_PUBLISH` as scientific reader content.
- Failed attempts, noncanonical aborts, preflights, stage-boundary smokes: `DO_NOT_PUBLISH`; retain internally for engineering audit only.
- Prime Editing Transfer 2 decision, source-gate, assay-validity, and nonidentifiable closure series: `READY_LAB_INTERNAL` only after a separate privacy review; not public in this phase.
- Super Mitochondria, TALED, PRAME, and xrRNA scientific series not listed in the priority table: `REQUIRES_REDACTION` or `HISTORICAL_ONLY` pending selection of one canonical publication object.
- Release candidates and observability packages: `READY_LAB_INTERNAL` at most; they are runtime artifacts, not reader-facing science.

This pattern-level classification covers the current run-root inventory without copying it into the portal. New directories default to `DO_NOT_PUBLISH` until explicitly inventoried.

## Demo mapping

| Demo bundle | Status shown | Purpose |
|---|---|---|
| `xrrna-prime-assembly-demo` | Done | `FOCUSED_DECISION_RUN`: PRIMARY symmetric motif panel, CONDITIONAL arm assignment, MEASUREMENT_PROGRAM E_A architecture, and PARKED QuadPE extension. Only PRIMARY has a dedicated final Idea Report PDF. |
| `prame-logic-first-demo` | Review required | `FOCUSED_DECISION_RUN`: primary context gate plus conditional verifier contrast; no broad discovery claim. |
| `taled-historical-demo` | Archived | One synthetic child-run lineage with exactly 2 generated families, 0 Arena entrants, 0 matches, and 0 finalists. |

The xrRNA fixture is not described as Arena-selected top-1. Missing PDFs never imply missing research objects.
